/**
 * Rebuild home page so-quad-ring SVGs (mono ring + center icon).
 * Run: node scripts/rebuild-home-quad-rings.mjs
 * This file re-exports the same behavior for backwards compatibility.
 */
import './rebuild-home-quad-rings.mjs';
