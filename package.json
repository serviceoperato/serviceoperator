{
  "name": "serviceopera-site",
  "version": "1.2.3",
  "private": true,
  "type": "module",
  "description": "Static site served by Node (Express); admin OTP via Resend when RESEND_API_KEY is set. Includes POST /api/places/search (Google Places API New) for Pattaya lead collection — see PLACES_LEADS.md.",
  "scripts": {
    "start": "node server.mjs",
    "preview:static": "npx --yes serve@14 . -l tcp://0.0.0.0:3000"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.16.0"
  }
}
