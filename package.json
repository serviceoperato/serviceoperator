{
  "name": "serviceopera-site",
  "version": "1.0.2",
  "private": true,
  "description": "Static site; package.json exists only so Railway/Nixpacks can run a static file server (no bundler).",
  "scripts": {
    "start": "npx --yes serve@14 . -l tcp://0.0.0.0:$PORT"
  }
}
